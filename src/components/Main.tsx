import React, { FC, useState } from "react";
import Header from "./Header";

const Main: FC = () => {
  return (
    <div>
      <div className="main_grid">로그인해주세요</div>
    </div>
  );
};

export default Main;
